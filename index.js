// Import prompts file in order to use the init function.
const init = require('./lib/Prompts');

// Calls the functions and the prompts run once the command 'node index.js' is run in the terminal.
init();